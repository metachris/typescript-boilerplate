// const b = require("benny");
// const { delayMillis } = require("../dist/tsc/main");

// b.suite(
//   "Example",
//   b.add("delayMillis", async () => {
//     await delayMillis(1000);
//   }),

//   b.cycle(),
//   b.complete(),
//   b.save({ file: "greet", version: "1.0.0" }),
//   b.save({ file: "greet", format: "chart.html" })
// );
